// Global dependencies
import React, { Component } from 'react';

// Importing components from the files
import './Footer.css';

// Created a component named Footer and extends by telling the component this is a class
class Footer extends Component {

// Imported components into Footer.js to be rendered
  render() {

// Return everything in between a component's opening and closing tags
    return (
        <footer className="Footer">
        </footer>

    );
  }
}

// Exporting to give access to use Footer, allows other indexes to reference Footer
export default Footer;
